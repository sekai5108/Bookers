

<body>
  <p id="notice"></p>
  <h1>Books</h1>
    <table>
      <thead>
        <tr>
          <th>Title</th>
          <th>body</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @books.each do |book| %>
        <tr>
          <td><%= book.title %></td>
          <td><%= book.body %></td>
          <td><%= link_to "Show", "/books/#{book.id}" %></td>
          <td><%= link_to "Edit", "/books/#{book.id}/edit" %></td>
          <td><%= link_to "Destroy", "destroy" %></td>
        <% end %>
        </tr>
      </tbody>

    </table>





</body>